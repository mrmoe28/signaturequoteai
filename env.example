# =============================================================================
# SignatureQuoteAI Environment Configuration
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to version control!

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development
SUPPORT_EMAIL=your-support@example.com

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Neon PostgreSQL connection string
# Format: postgresql://[user]:[password]@[host]/[database]?sslmode=require
DATABASE_URL=your_database_url_here

# =============================================================================
# EMAIL CONFIGURATION (Gmail SMTP)
# =============================================================================
# Gmail address to send emails from
GOOGLE_CLIENT_EMAIL=your-gmail@gmail.com

# Gmail App-Specific Password (NOT your regular password)
# Get this from: https://myaccount.google.com/apppasswords
GOOGLE_APP_PASSWORD=your-app-specific-password

# =============================================================================
# SQUARE PAYMENT INTEGRATION
# =============================================================================
# Square Environment: 'sandbox' for testing, 'production' for live payments
SQUARE_ENVIRONMENT=sandbox

# Square Application ID (from Square Developer Dashboard)
SQUARE_APPLICATION_ID=your_square_application_id

# Square Access Token (from Square Developer Dashboard > Credentials)
# Sandbox token starts with: EAAAl...
# Production token starts with: EAAAE...
SQUARE_ACCESS_TOKEN=your_square_access_token

# Square Location ID (from Square Developer Dashboard > Locations)
SQUARE_LOCATION_ID=your_square_location_id

# Square Webhook Signature Key (from Square Developer Dashboard > Webhooks)
# This is used to verify webhook authenticity
SQUARE_WEBHOOK_SIGNATURE_KEY=your_webhook_signature_key

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================
# Logging level: 'debug', 'info', 'warn', 'error'
LOG_LEVEL=info

# Session secret for authentication (generate a random string)
# You can generate one with: openssl rand -base64 32
SESSION_SECRET=your_random_session_secret_here

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file: cp env.example .env.local
# 2. Fill in all the values (replace 'your_*' placeholders)
# 3. Validate your setup: npm run env:validate
# 4. Start development: npm run dev
#
# For detailed setup guides, see:
# - docs/VERCEL_ENVIRONMENT_SETUP.md (Database & Deployment)
# - docs/SQUARE_PAYMENT_SETUP.md (Payment Integration)
# - docs/EMAIL_SETUP.md (Email Configuration)
# - docs/GMAIL_SMTP_SETUP.md (Gmail App Password)

